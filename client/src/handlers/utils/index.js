const logsHandler = require('./logs');
const optionsHandler = require('./options');
const terminateConnectionHandler = require('./terminateConnection');

module.exports = {
  logsHandler,
  optionsHandler,
  terminateConnectionHandler,
};
